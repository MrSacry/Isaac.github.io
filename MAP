<!DOCTYPE html>
<html>

<head>
    <meta charset=utf-8 />
    <title>My Deeper Cut Map</title>

    <!-- The map will be drawn inside this div -->
    <div id="mapid"></div>

    <!-- Makes the map responsive on different screen sizes -->
    <meta name='viewport' content='initial-scale=1,maximum-scale=1,user-scalable=no' />

    <!-- Leaflet CSS controls how the map looks -->
    <link rel="stylesheet" href="https://unpkg.com/leaflet@1.4.0/dist/leaflet.css" crossorigin="" />

    <!-- Leaflet JavaScript controls how the map works -->
    <script src="https://unpkg.com/leaflet@1.4.0/dist/leaflet.js" crossorigin=""></script>

    <style>
        /* This CSS ensures the map fills the entire browser window */
        #mapid {
            position: absolute;
            top: 0;
            bottom: 0;
            right: 0;
            left: 0;
        }
    </style>
</head>

<body>

<script>

    /*
    The map object is created here.
    setView() determines the center point and zoom level.
    These coordinates are centered on my hometown:
    3039 Goezel Rd, Perkiomenville, PA
    */
    var mymap = L.map('mapid').setView([40.3046, -75.5021], 15);

    /*
    This is the base map layer.
    I changed the basemap from the default to OpenStreetMap.
    Basemaps provide geographic context such as roads and terrain.
    */
    var tiles = L.tileLayer(
        'https://{s}.tile.openstreetmap.org/{z}/{x}/{y}.png',
        {
            maxZoom: 19,
            attribution: '&copy; OpenStreetMap contributors'
        }
    ).addTo(mymap);

    /*
    Markers are used to represent a single point location.
    This marker represents my home.
    */
    var marker = L.marker([40.3046, -75.5021]).addTo(mymap);

    /*
    Circles are useful for showing areas of influence or distance.
    This circle represents my neighborhood area.
    I changed the color, doubled the radius, set fillOpacity to 1,
    and added an extra property (dashArray).
    */
    var circle = L.circle([40.3046, -75.5021], {
        color: 'blue',          // Changed from red
        fillColor: 'lightblue',
        fillOpacity: 1,         // Fully opaque
        radius: 1000,           // Doubled size
        dashArray: '5,5'        // Extra property/value pair
    }).addTo(mymap);

    /*
    Polygons are used to represent custom-shaped areas.
    This polygon represents a nearby area I spend time in.
    */
    var polygon = L.polygon([
        [40.3065, -75.5050],
        [40.3020, -75.5080],
        [40.3005, -75.5010]
    ]).addTo(mymap);

    /*
    Tooltips are different from popups.
    Tooltips appear on hover and do not block interaction.
    Popups require clicking and temporarily stop map interaction.
    I replaced popups with tooltips and made the text bold.
    */

    marker.bindTooltip("<b>My Home – 3039 Goezel Rd</b>");
    circle.bindTooltip("<b>My Neighborhood Area</b>");
    polygon.bindTooltip("<b>Nearby Area I Spend Time In</b>");

</script>

</body>
</html>
